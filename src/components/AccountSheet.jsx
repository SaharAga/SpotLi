/**
 * The Account screen.
 *
 * Consolidates the account & settings views into a single responsive modal
 * without nested modal portals or duplicated styles.
 */
export { AccountModal as AccountSheet } from './AccountModal';

