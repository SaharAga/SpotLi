---
type: patch
---

Gmail refresh tokens are now encrypted at rest. The Gmail push endpoint accepts Pub/Sub OIDC authentication. The 17TRACK webhook finds packages with one indexed query. `googleapis` was upgraded, leaving no `npm audit` findings in functions. The CSP no longer allows inline scripts.
